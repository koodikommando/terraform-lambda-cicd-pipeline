{
  "name": "password-generator-lambda",
  "version": "0.1.0",
  "description": "Cryptographically secure password generator, built as a learning exercise for a Lambda/Terraform/CI-CD pipeline.",
  "type": "module",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "build": "tsc",
    "demo": "tsx scripts/demo.ts",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "devDependencies": {
    "@types/node": "^20.14.0",
    "tsx": "^4.16.5",
    "typescript": "^5.5.4",
    "vitest": "^2.0.5"
  }
}
